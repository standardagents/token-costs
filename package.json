{
  "name": "are-tokens-getting-cheaper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.3",
    "vite": "^8.0.11",
    "wrangler": "^4.90.0"
  }
}
